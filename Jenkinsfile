pipeline {
    agent any
    stages {
        stage('Afficher la date') {
            steps {
                script {
                    def currentDate = new Date()
                    echo "La date actuelle est : ${currentDate}"
                }
            }
        }
    }
}
